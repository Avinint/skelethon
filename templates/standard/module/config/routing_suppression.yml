json_suppression_TABLE:
    reecriture: admin/mODULE/TABLE/(\d+)/suppression.json
    route:
        zone: admin
        module: mODULE
        controller: cONTROLLER
        action: suppression
        variables:
            - nIdElement
