son_suppression_TABLE:
    reecriture: admin/mODULE/TABLE/(\d+)/suppression.json
    route:
        zone: admin
        module: mODULE
        controller: TABLE
        action: suppression
        variables:
            - nIdElement